// No migrations yet. Generate the first one against a fresh database with:
//   pnpm --filter @treenweb/backend run migrate:create initial
// (dev auto-syncs the schema via PAYLOAD_DB_PUSH=true; prod applies these).
export const migrations = [];
